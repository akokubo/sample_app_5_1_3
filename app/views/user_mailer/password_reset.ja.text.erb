パスワードを再設定するには、以下のリンクをクリックしてください:

<%# email: @user.emailを追加すると、リンクしたURLに?email=@user.emailが追加される -%>
<%= edit_password_reset_url(@user.reset_token, email: @user.email, locale: I18n.locale) %>

このリンクの有効期限は2時間です。

もしも、パスワード再設定の申し込みをしていない場合は、このメールを無視してください。
パスワードは変更されません。
