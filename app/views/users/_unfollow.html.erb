<%# フォローする関係から対象のユーザーを検索し、それにAjaxでDELETEを送信するフォームを作る -%>
<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit t('.unfollow'), class: "btn" %>
<% end %>
