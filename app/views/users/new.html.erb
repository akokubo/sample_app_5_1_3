<% provide(:title, 'Sign up') %>
<% provide(:button_text, 'Create my account') %>
<h1>Sign up</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%# 登録に失敗した場合にも/usersにリダイレクトするのでなく、/signupにする -%>
    <%= render 'form', options: { url: signup_path } %>
  </div>
</div>
